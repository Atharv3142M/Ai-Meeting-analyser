// This script is injected into all pages, as defined in the manifest.
// It's not actively used in this workflow, but is required by the manifest.
console.log('Local AI Recorder content script loaded.');